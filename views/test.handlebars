<div class="row">
    <div class="col s10">
        {{!-- <h5 id="selectedBook" value={{id}}>{{title}}</h5> --}}
        <input id="selectedBook" value="{{title}}">
        {{!-- <p>By: {{author}}</p> --}}
        <input id="bookAuthor" value="{{author}}">

         <div class="input-field col s12">
            <textarea id="selectedBook" class="materialize-textarea" value={{id}}>{{author}}</textarea>
            <label for="clubName">Club Name</label>
        </div>


    </div>
    <div class="col s1">
        <a class="material-icons right blue-text modal-trigger" href="#modalEditNovel" id="editNovel">create</a>
    </div>
    <div class="col s1" id="deleteNovel">
        <a class="material-icons right blue-text" href="#">delete</a>
    </div>
</div>




{{#each books}}
    <div class="row">
        <div class="col s10">
            <h5 id="selectedBook" value={{id}}>{{title}}</h5>
            <p>By: {{author}}</p>


        </div>
        <div class="col s1" id="editNovel">
            <a class="material-icons right blue-text modal-trigger" href="#modalEditNovel" id="editNovel">create</a>
        </div>
        <div class="col s1" >
            <a class="material-icons right blue-text" href="#!" id="deleteNovel">delete</a>
        </div>
    </div>
    <hr>
{{/each}}
