{{!-- This code is incomplete. Reference the TODELETEvote.html file. --}}
{{> navbar}}
<section>
    <div class="container">
        <div class="row">
            <div class="col s2"></div>
            <div class="col s8">
                <h1>Vote from selections below for this month! </h1>
                <div class="with-gap">
                    {{#each books}}
                        <p>
                            <label>
                                <input class="with-gap" name="group3" type="radio" checked />
                                <span>{{title}}</span>
                            </label>
                        </p>
                    {{/each}}
                </div>

                        <button class="btn waves-effect waves-light" type="submit" name="action">Submit
                             <i class="material-icons right">send</i>
                        </button>
            </div>
            <div class="col s2"></div>
        </div>
        <div class="row">
            <div class="col s2"></div>
            <div class="col s8">
                {{!-- book-block will be injected here. --}}
                {{!-- {{books}} --}}
            </div>
            <div class="col s2"></div>
        </div>
    </div>
</section>